{
  "name": "claude-usage-mcp",
  "version": "0.1.0",
  "description": "MCP server that reports Claude subscription usage (5-hour + weekly) with a forecast and a velocity recommendation, reusing the OAuth session Claude Code already stores. No API key required.",
  "type": "module",
  "bin": {
    "claude-usage-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "typescript": "^5.6.0"
  },
  "license": "MIT"
}
